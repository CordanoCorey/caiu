import { Store } from '@ngrx/store';
import { Observable } from 'rxjs';
import { Error } from './errors.models';
import { Dictionary } from '../shared/models';
export declare function errorsSelector(store: Store<any>): Observable<Dictionary<Error>>;
export declare function errorSelector(store: Store<any>, key: string): Observable<Error>;
