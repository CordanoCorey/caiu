import { Store } from '@ngrx/store';
import { Observable } from 'rxjs';
import { Event } from './events.models';
import { Dictionary } from '../shared/models';
export declare function eventsSelector(store: Store<any>): Observable<Dictionary<Event>>;
export declare function eventSelector(store: Store<any>, key: string): Observable<Event>;
export declare function eventValueSelector(store: Store<any>, key: string): Observable<any>;
